export enum SupportedLanguage {
  En = "en",
  Pl = "pl"
}

export enum TranslationNamespace {
  Feedback = "feedback",
  Common = "common",
  Ui = "ui"
}

export enum SliceRange {
  begin = 0,
  end = 2
}
